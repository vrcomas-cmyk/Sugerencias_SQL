# Secretos reales — nunca subir. .env.example es la plantilla pública.
.env
.env.local

__pycache__/
*.pyc

# Log de uvicorn corriendo local, no es código.
uvicorn.log
api_log.txt

.venv/
venv/

# Artefactos de PyInstaller — el .exe se distribuye vía GitHub Releases, no en git.
build/
dist/
